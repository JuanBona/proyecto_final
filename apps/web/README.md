# Web app (Next.js)

Frontend del monorepo.

## Desarrollo local

Desde la raíz del repositorio:

```bash
npm install
npm run dev
```

La web corre en `http://localhost:3000`.
La API corre en `http://localhost:3001` por defecto.

Para ejecutar solo la web:

```bash
npm run dev -w apps/web
```
