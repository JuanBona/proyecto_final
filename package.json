{
  "name": "proyecto-final-viajes",
  "private": true,
  "workspaces": ["apps/*"],
  "packageManager": "npm@11.4.1",
  "engines": {
    "node": "22.x",
    "npm": "11.x"
  },
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
